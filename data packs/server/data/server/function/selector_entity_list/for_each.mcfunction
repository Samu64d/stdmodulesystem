#
# server:selector_entity_list/for_each
#
# Iterate through the selector entity list
#> Storage
#  @callback (in): the callback function
#> Return void

# Iterate through the entity list
execute as @e run function server:selector_entity_list/for_each/callback with storage io:
