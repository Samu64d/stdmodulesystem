#
# server:selector_player_list/for_each
#
# Iterate through the selector player list
#> Storage
#  @callback (in): the callback function
#> Return void

# Iterate through the player list
execute as @a run function server:selector_player_list/for_each/callback with storage io:
