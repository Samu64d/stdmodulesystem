#
# world_handler:ticking_area_chunks_manager/chunk_event_list/is_initialized
#
# Check if the chunk event list is initialized
#> Return data
#  boolean flag

return run execute if data storage world_handler:data ticking_area_chunks_manager.chunk_event_list
