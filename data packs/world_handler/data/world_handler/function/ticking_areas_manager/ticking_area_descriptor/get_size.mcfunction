#
# world_handler:ticking_areas_manager/ticking_area_descriptor/get_size
#
# Get the size of a ticking area descriptor
#> Storage
#  @ticking_area_descriptor (in): the ticking area descriptor
#> Return data
#  size value

return run data get storage io: ticking_area_descriptor.size 1.0
