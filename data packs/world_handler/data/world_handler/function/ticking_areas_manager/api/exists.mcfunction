#
# world_handler:ticking_areas_manager/api/exists
#
# Check if a ticking area exists
#> Storage
#  @uuid (in): the ticking area uuid
#> Return void

return run function world_handler:ticking_areas_manager/main/exists_ticking_area
