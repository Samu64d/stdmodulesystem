#
# world_handler:ticking_areas_manager/api/create
#
# Create a new ticking area and return it's uuid
#> Scores
#  @center_x (in): the x center position
#  @center_z (in): the z center position
#  @size (in): the size
#  @forceloaded (in): boolean flag
#> Storage
#  @dimension (in): the dimension
#  @uuid (out): the ticking area uuid
#> Return void

function world_handler:ticking_areas_manager/main/create_ticking_area
