#
# obj:bind
#
# Bind a runtime method with an object pointer
#> Storage
#  @pointer (in): the object pointer
#  @method (in): the method name
#> Return data
#  boolean flag

return run function obj:bind/bind with storage io:
